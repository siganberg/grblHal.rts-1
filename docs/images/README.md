# README images — to add

Drop these screenshots/photos here so the flashing guide in the main
[`README.md`](../../README.md) renders them. Keep the exact filenames.

| Filename | What to show |
|---|---|
| `boot0-button.png` | Photo of the open controller with the **BOOT0** button clearly circled/arrowed. |
| `boot0-usb-dfu.png` | Holding BOOT0 while plugging in USB-C — and/or the PC showing the *STM32 BOOTLOADER* / `0483:DF11` DFU device. |
| `ncsender-flash.png` | ncSender **Settings → Firmware → Flash Firmware** dialog (selecting the `.hex`). |

Tips for friendly screenshots: crop tight, add a colored circle/arrow on the key element,
and keep them a reasonable width (~800–1000 px). PNG or JPG is fine.
